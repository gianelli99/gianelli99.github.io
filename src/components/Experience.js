import React from 'react';
import { Fade } from 'react-reveal';
import { OpeningTag } from './OpeningTag';
import { ClosingTag } from './ClosingTag';
import { Tag } from './Tag';

export const Experience = () => {
  return <Tag tagName="BiSports" InnerComponent={undefined} />;
};
